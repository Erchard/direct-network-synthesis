# Results

Raw experiment outputs may be written here during local runs, but generated result files are
ignored by Git by default.

Commit only curated summaries that include:

- command used;
- configuration path or inline configuration;
- commit SHA;
- split seeds;
- mean and standard deviation across splits;
- notes on whether each model used closed-form computation or iterative optimization.
